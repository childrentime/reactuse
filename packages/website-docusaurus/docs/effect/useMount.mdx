---
title: useMount – Effect Hook Usage & Examples
sidebar_label: useMount
description: React lifecycle hook that executes a function after the component is mounted. Learn usage patterns, best practices, and code examples for React developers.
---
# useMount

React lifecycle hook that executes a function after the component is mounted


## Usage

```tsx live
function Demo() {
  const [value, setValue] = useState("UnMounted");
  useMount(() => {
    setValue("Mounted");
  });
  return <div>{value}</div>;
};

```

%%API%%
