---
title: useIdle – Browser Hook Usage & Examples
sidebar_label: useIdle
description: React sensor hook that tracks if user on the page is idle. Learn usage patterns, best practices, and code examples for React developers.
---
# useIdle

React sensor hook that tracks if user on the page is idle


## Usage

```tsx live
function Demo() {
  const isIdle = useIdle(3e3);

  return (
    <div>
      <div>User is idle: {isIdle ? "Yes 😴" : "Nope"}</div>
    </div>
  );
};

```

%%API%%
