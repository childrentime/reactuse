---
title: usePageLeave – Browser Hook Usage & Examples
sidebar_label: usePageLeave
description: React sensor hook that tracks when mouse leaves the page. Learn usage patterns, best practices, and code examples for React developers.
---
# usePageLeave

React sensor hook that tracks when mouse leaves the page


## Usage

```tsx live
function Demo() {
  const isLeft = usePageLeave();

  return <div>isLeft: {JSON.stringify(isLeft)}</div>;
};

```

%%API%%
