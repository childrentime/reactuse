---
title: useDevicePixelRatio – Browser Hook Usage & Examples
sidebar_label: useDevicePixelRatio
description: >-
  `useDevicePixelRatio` is a hook that returns the device pixel ratio of the screen. Learn usage patterns, best practices, and code examples for React
  developers.
---
# useDevicePixelRatio

`useDevicePixelRatio` is a hook that returns the device pixel ratio of the screen.

## Usage

```tsx live
function Demo() {
  const { pixelRatio } = useDevicePixelRatio();

  return <p>Device pixel ratio: {pixelRatio}</p>;
};

```

%%API%%
