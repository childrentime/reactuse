---
title: useCycleList – State Hook Usage & Examples
sidebar_label: useCycleList
description: Cycle through a list of items. Learn usage patterns, best practices, and code examples for React developers.
---
# useCycleList

Cycle through a list of items


## Usage

```tsx live
function Demo() {
  const [state, next, prev] = useCycleList([
    "Dog",
    "Cat",
    "Lizard",
    "Shark",
    "Whale",
    "Dolphin",
    "Octopus",
    "Seal",
  ]);

  return (
    <div>
      <div>{state}</div>
      <div>
        <button onClick={() => next()}>next</button>
        <button onClick={() => prev()}>prev</button>
      </div>
    </div>
  );
};

```

%%API%%
