---
title: useDoubleClick 用法與示例
sidebar_label: useDoubleClick
description: 雙擊跟蹤。 本文介紹其用法、最佳實踐與代碼示例。
---
# useDoubleClick

雙擊跟蹤

## Usage

```tsx live

function Demo() {
  const element = useRef<HTMLButtonElement>(null);
  const [text, setText] = useState("未點擊");

  useDoubleClick({
    target: element,
    onSingleClick: () => {
      setText("single click");
    },
    onDoubleClick: () => {
      setText("double click");
    },
  });
  return (
    <div>
      <button ref={element}>點擊我</button>
      <p>{text}</p>
    </div>
  );
};

```

%%API%%