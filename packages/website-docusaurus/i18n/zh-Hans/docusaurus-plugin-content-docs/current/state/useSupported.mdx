---
title: useSupported 用法与示例
sidebar_label: useSupported
description: 检测浏览器是否支持某些新的特性。 本文介绍其用法、最佳实践与代码示例。适用于 React 开发中需要处理副作用的场景。
---
# useSupported

检测浏览器是否支持某些新的特性

## Usage

```tsx live

function Demo() {
  const isSupported = useSupported(() => "EyeDropper" in window);
  return (
    <div>
      <p>
        window.EyeDropper is {isSupported ? "supported" : "unsupported"} in your
        browser
      </p>
    </div>
  );
};

```

%%API%%