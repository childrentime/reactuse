---
title: useLocalStorage 用法与示例
sidebar_label: useLocalStorage
description: 轻松管理 `localStorage`。 本文介绍其用法、最佳实践与代码示例。适用于 React 开发中需要处理副作用的场景。
---
# useLocalStorage

轻松管理 `localStorage`

## Usage

```tsx live

function Demo() {
  // bind string
  const [value, setValue] = useLocalStorage("my-key", "key");

  return (
    <div>
      <div>Value: {value}</div>
      <button onClick={() => setValue("bar")}>bar</button>
      <button onClick={() => setValue("baz")}>baz</button>
      {/* delete data from storage */}
      <button onClick={() => setValue(null)}>Remove</button>
    </div>
  );
};

```

%%API%%