---
title: useFirstMountState 用法与示例
sidebar_label: useFirstMountState
description: 返回组件是否刚刚挂载的 React Hook。 本文介绍其用法、最佳实践与代码示例。适用于 React 开发中需要处理副作用的场景。
---
# useFirstMountState

返回组件是否刚刚挂载的 React Hook

## Usage

```tsx live

function Demo() {
  const isFirstMount = useFirstMountState();
  const [render, reRender] = useState(0);

  return (
    <div>
      <span>This component is just mounted: {isFirstMount ? "YES" : "NO"}</span>
      <br />
      <button onClick={() => reRender(1)}>{render}</button>
    </div>
  );
};

```

%%API%%