---
title: useClickAway – Element Hook 使用方法与示例 (useClickOutside 的别名)
sidebar_label: useClickAway
description: 监听元素外部的点击事件。useClickOutside 的别名。适用于模态框或下拉菜单。学习使用模式、最佳实践和 React 开发者的代码示例。
---
# useClickAway

监听元素外部的点击事件。适用于模态框或下拉菜单。

:::info 别名
`useClickAway` 是 [`useClickOutside`](/zh-Hans/element/useClickOutside) 的别名。它们具有相同的功能和 API。
:::

## 用法

```tsx live
function Demo() {
  const [visible, setVisible] = useState(false);

  const modalRef = useRef<HTMLDivElement>(null);

  useClickAway(modalRef, () => {
    setVisible(false);
  });

  return (
    <div>
      <button onClick={() => setVisible(true)}> 打开模态框</button>
      {visible && (
        <div
          ref={modalRef}
          style={{
            position: "fixed",
            left: "50%",
            top: "50%",
            transform: "translate(-50%,-50%)",
            width: 420,
            maxWidth: "100%",
            zIndex: 10,
            border: "1px solid var(--c-input-border)",
            background: "var(--c-bg)",
          }}
        >
          <div
            style={{
              padding: "0.4em 2em",
              boxShadow: "2px 2px 10px var(--c-box-shadow)",
            }}
          >
            <p
              style={{
                fontWeight: 700,
                fontSize: "1.4rem",
                marginBottom: "2rem",
              }}
            >
              演示模态框
            </p>
            <p>点击模态框外部关闭它。</p>
          </div>
        </div>
      )}
    </div>
  );
};

```

%%API%%
