---
title: useActiveElement 用法与示例
sidebar_label: useActiveElement
description: React 跟踪 `document.activeElement` 的监听 hook。 本文介绍其用法、最佳实践与代码示例。
---
# useActiveElement

React 跟踪 `document.activeElement` 的监听 hook


## Usage

```tsx live noInline

function Demo() {
  const arr = [1, 2, 3, 4, 5, 6];

  const activeElement = useActiveElement<HTMLElement>();

  const key = useMemo(() => {
    return activeElement?.dataset?.id;
  }, [activeElement?.dataset?.id]);

  return (
    <div>
      <p>选择下面的输入框以查看变化</p>
      <div style={{ display: "flex", gap: 15, flexWrap: "wrap" }}>
        {arr.map((i) => {
          return <input key={i} placeholder={`${i}`} data-id={i} />;
        })}
      </div>
      <br />
      <div>
        当前活动元素:
        <span> {activeElement?.tagName}</span>
      </div>
       <div>
        当前活动元素的data-key:
        <span>{key}</span>
      </div>
    </div>
  );
};

render(<Demo/>)
```



%%API%%