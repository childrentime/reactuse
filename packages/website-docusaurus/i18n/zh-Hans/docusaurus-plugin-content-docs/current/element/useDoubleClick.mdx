---
title: useDoubleClick 用法与示例
sidebar_label: useDoubleClick
description: 双击跟踪。 本文介绍其用法、最佳实践与代码示例。适用于 React 开发中需要处理副作用的场景。
---
# useDoubleClick

双击跟踪

## Usage

```tsx live

function Demo() {
  const element = useRef<HTMLButtonElement>(null);
  const [text, setText] = useState("no click");

  useDoubleClick({
    target: element,
    onSingleClick: () => {
      setText("single click");
    },
    onDoubleClick: () => {
      setText("double click");
    },
  });
  return (
    <div>
      <button ref={element}>Click Me</button>
      <p>{text}</p>
    </div>
  );
};

```

%%API%%