---
title: useDebounceFn 用法与示例
sidebar_label: useDebounceFn
description: 使用React hooks防抖（debounce）函数。 本文介绍其用法、最佳实践与代码示例。适用于 React 开发中需要处理副作用的场景。
---
# useDebounceFn

使用React hooks防抖（debounce）函数

## Usage

```tsx live

function Demo() {
  const [value, setValue] = useState(0);
  const { run } = useDebounceFn(() => {
    setValue(value + 1);
  }, 500);

  return (
    <div>
      <p style={{ marginTop: 16 }}> Clicked count: {value} </p>
      <button type="button" onClick={run}>
        Click fast!
      </button>
    </div>
  );
};

```

%%API%%