---
title: useWebNotification 用法与示例
sidebar_label: useWebNotification
description: 通知 API 的 Web 通知接口用于配置并向用户显示桌面通知。 :::warning 注意：对于Mac系统的用户，需要在系统设置中启用Chrome的通知功能。 如果系统中没有启用Chrome的通知，即使前端页面请求权限成功，也不会显示通知。 本文介绍其用法、最佳实践与代码示例。
---
# useWebNotification

通知 API 的 Web 通知接口用于配置并向用户显示桌面通知。

:::warning
注意：对于Mac系统的用户，需要在系统设置中启用Chrome的通知功能。 如果系统中没有启用Chrome的通知，即使前端页面请求权限成功，也不会显示通知。
:::

## Usage

```tsx live

function Demo() {
  const { isSupported, show, close }
    = useWebNotification(true);
  return (
    <div>
      <p>支持状态: {JSON.stringify(isSupported)}</p>
      {isSupported
        ? (
          <div>
            <button
              onClick={() => {
                show("你好，来自 ReactUse 的问候！");
              }}
            >
              显示通知
            </button>
            <button onClick={close}>关闭</button>
          </div>
          )
        : (
          <div>您的浏览器不支持通知 Web API。</div>
          )}
    </div>
  );
}

```

%%API%%