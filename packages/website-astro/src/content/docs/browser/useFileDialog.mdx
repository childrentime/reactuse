---
title: useFileDialog – Browser Hook Usage & Examples
sidebar_label: useFileDialog
description: Open file dialog with ease. Learn usage patterns, best practices, and code examples for React developers.
---
# useFileDialog

Open file dialog with ease


## Usage

```tsx live
function Demo() {
  const [files, open, reset] = useFileDialog();

  return (
    <div>
      <button onClick={() => open()}> Choose files</button>
      <button
        style={{ marginLeft: 20 }}
        disabled={!files}
        onClick={() => {
          reset();
        }}
      >
        Reset
      </button>
      {files && (
        <div>
          <p>
            You have selected: <b>{files.length} files</b>
            {Array.from(files).map((file) => {
              return <li key={file.name}>{file.name}</li>;
            })}
          </p>
        </div>
      )}
    </div>
  );
};

```

%%API%%
