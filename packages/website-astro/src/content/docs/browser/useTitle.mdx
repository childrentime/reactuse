---
title: useTitle – Browser Hook Usage & Examples
sidebar_label: useTitle
description: set document title. Learn usage patterns, best practices, and code examples for React developers.
---
# useTitle

set document title.

## Usage

```tsx live
function Demo() {
  const [title, setTitle] = useState("title");

  useTitle(title);

  return (
    <div>
      <button onClick={() => setTitle("newTitle")}>Change Title</button>
    </div>
  );
};

```

%%API%%
