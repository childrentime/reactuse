---
title: useElementBounding 用法與示例
sidebar_label: useElementBounding
description: 追踪 HTML 元素边界框的 React Hook。 本文介紹其用法、最佳實踐與代碼示例。
---
# useElementBounding

追踪 HTML 元素边界框的 React Hook

## Usage

```tsx live

function Demo() {
  const ref = useRef<HTMLTextAreaElement>(null);
  const rect = useElementBounding(ref);
  return (
    <div>
      <p>調整方框大小以查看變化</p>
      <textarea ref={ref} readOnly value={JSON.stringify(rect, null, 2)} />
    </div>
  );
};

```

%%API%%