### useBatchedMount

#### Returns
`number`: 已挂载的项目数

#### Arguments
|参数名|描述|类型|默认值|
|---|---|---|---|
|total|需要挂载的项目总数。增大时保留已挂载的进度；减小时收敛到新的总数，而不是从第一批重新开始|number  **(必填)**|-|
|batchSize|每个空闲帧挂载的项目数。小于 1 的值按 1 处理，因此由尚未完成的测量得出的 batchSize 不会让列表永久停滞|number  **(必填)**|-|