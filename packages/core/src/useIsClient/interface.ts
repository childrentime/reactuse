/**
 * @title useIsClient
 * @returns_en Whether the code is running in the browser (client-side)
 * @returns_zh 代码是否在浏览器（客户端）中运行
 * @returns_zh-Hant 代碼是否在瀏覽器（客戶端）中運行
 */
export type UseIsClient = () => boolean
