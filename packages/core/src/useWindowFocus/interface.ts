/**
 * @title useWindowsFocus
 * @returns 窗口是否聚焦
 * @returns_en whether window focus
 */
export type UseWindowsFocus = (
  /**
   * @zh 默认值
   * @en defauleValue
   */
  defauleValue?: boolean
) => boolean;
